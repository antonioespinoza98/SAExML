---
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r}
rm(list = ls())
```

# Librerias

```{r}
x <- c('rpart','ggplot2','dplyr')
lapply(x, require, character.only = TRUE)
```

# Base de datos

```{r}
load("./data/ingreso/encuesta_df_agg.Rdata")
```

# Base de datos de entrenamiento y prueba

```{r, warning=FALSE}
RNGkind(sample.kind = "Rounding")
```

```{r}
set.seed(10)
n = nrow(encuesta_df_agg)
s = sample(1:n, round(0.8*n))

train = encuesta_df_agg[s, ]
test = encuesta_df_agg[-s, ]
```

```{r}
attach(encuesta_df_agg)
names(encuesta_df_agg)
pairs(encuesta_df_agg[,-c(1,3,4,5,6,7,8)])

base <- encuesta_df_agg %>%
    select(-depto, -sexo, -anoest,
    -edad, -n, -dam, -ingreso)

```

```{r}

```

```{r}

```

```{r}

```
